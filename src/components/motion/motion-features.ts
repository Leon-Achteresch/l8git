// Loaded asynchronously by MotionProvider's LazyMotion so the animation
// runtime stays out of the entry chunk. domMax includes layout animations
// (layoutId is used across the app).
import { domMax } from "motion/react";

export default domMax;
